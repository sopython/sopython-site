[uwsgi]
base = /var/www/sopython

home = %(base)/venv
pythonpath = %(base)

wsgi-file = %(base)/main.py

touch-reload = %(base)/main.py

# Socket
socket = /var/www/%n_uwsgi.sock
chmod-socket = 666
uid = www-data
gid = www-data

# Logging
logto = /var/log/uwsgi/%n.log
